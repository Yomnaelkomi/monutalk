// ticket_details.dart
import 'package:flutter/material.dart';

class MyticketDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ticket Details')),
      body: Center(
        child: Text('Ticket Details Page'),
      ),
    );
  }
}
